<%= form_with model: @net_line do |f| %>

  <%= f.label :name, '回線' %>
  <%= f.text_field :name %>

  <%= f.label :service_id, 'サービス' %>
  <%= f.collection_select :service_id, Service.all, :id, :name %>

  <%= f.submit %>
<% end %>